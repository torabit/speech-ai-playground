# Phase 1 以降で使う。実際の値は server/.env に書く（gitignore 済み）
DEEPGRAM_API_KEY=
GEMINI_API_KEY=
# 既定値は gemini-3.1-flash-lite。2.5-flash-lite はこの鍵では 404（新規キーには提供終了）、
# 3.5-flash-lite は実測時点で HTTP 503（高負荷）が続いていて使えなかった
GEMINI_MODEL=
# 既定値は aura-2-thalia-en
DEEPGRAM_TTS_MODEL=
